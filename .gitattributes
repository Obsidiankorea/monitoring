# 배치파일은 CP949(한국어 ANSI)로 둔다.
# cmd 가 콘솔 코드페이지로 읽으므로 UTF-8 로 저장하면 글자가 깨지고,
# 깨진 바이트가 파싱까지 망가뜨려 실행이 중간에 멈춘다.
# git 이 내용을 건드리지 않도록 -text 로 못 박는다.
*.bat -text

# 맥·리눅스 스크립트는 LF 여야 한다. CRLF 로 체크아웃되면 셔뱅이 깨진다.
*.command text eol=lf
*.sh      text eol=lf
